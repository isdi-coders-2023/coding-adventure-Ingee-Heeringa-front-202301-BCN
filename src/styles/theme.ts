export interface CustomTheme {
  colors: {
    primary: string;
    accent: string;
  };
  fonts: {
    primary: string;
    secondary: string;
  };
}

export const theme = {
  colors: {
    primary: "#1E1E1E",
    accent: "#CCCF18",
  },
  fonts: {
    primary: "Roboto",
    secondary: "Saira Stencil One",
  },
};
